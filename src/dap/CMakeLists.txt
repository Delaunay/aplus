# better way ?
INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(..)

file(GLOB DAP_SRC *.c)
file(GLOB DAP_HDS *.h)

ADD_LIBRARY(dap SHARED ${DAP_HDS} ${DAP_SRC})
SET_PROPERTY(TARGET dap PROPERTY CXX_STANDARD 11)
